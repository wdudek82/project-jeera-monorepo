import { UniqueEmailValidator } from './unique-email.validator';

describe('UniqueEmail', () => {
  xit('should create an instance', () => {
    // TODO: Fix dependency injection in test
    // expect(new UniqueEmailValidator()).toBeTruthy();
  });
});
