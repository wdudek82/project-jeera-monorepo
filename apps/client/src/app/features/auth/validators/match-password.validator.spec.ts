import { MatchPasswordValidator } from './match-password.validator';

describe('MatchPassword', () => {
  it('should create an instance', () => {
    expect(new MatchPasswordValidator()).toBeTruthy();
  });
});
